module.exports = function(options) {

  let coord = []
  coord.push(options.nw.split(' '))
  coord.push(options.ne.split(' '))
  coord.push(options.sw.split(' '))
  coord.push(options.se.split(' '))

  let parsedCoord = coord.reduce()

}